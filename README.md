# CookBuddy
HCI (CS 330) Team Project - Anant, Anton, Ethan, Vivian

## Setup
1. Clone the repo
2. Run `npm install`
3. Add a `.env` file (ask a teammate for the Firebase config)
4. Run `npm run dev`
